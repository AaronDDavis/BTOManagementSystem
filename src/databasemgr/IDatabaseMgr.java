package databasemgr;

/**
 * A generic interface for a database manager.
 * <p>
 * This interface serves as a marker for classes that manage the interaction with
 * a database or a data repository of a specific type. It is a base contract
 * for database managers, ensuring that they conform to a common structure.
 * </p>
 */
public interface IDatabaseMgr<T> {
	// No methods are currently defined in this interface.
}